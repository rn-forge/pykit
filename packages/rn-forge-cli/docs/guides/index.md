# Guides

Start here for the main usage flows:

- [installation](installation.md) and what this package is for
- [CLI](cli.md) wiring: `CliApp`, the standard options, exit codes, and how
  commands read the resolved flags
- [declaring a CLI](declaring.md): describing a command surface in
  configuration instead of writing it

Console output — the `AppConsole` facade and its RICH/PLAIN/QUIET/JSON modes —
is documented in `rn-forge-commons`, which owns it.
