"""Filesystem mechanisms: paths, hashes, locks, fenced blocks and documents.

Everything here reads or writes a real file. Import the modules directly; the
curated names are re-exported from :mod:`rn_forge.commons`.
"""
