# Guides

Start here for the main usage flows:

- installation and optional extras
- common imports and quickstart patterns
- the `RN_FORGE_DJANGO` settings facade
- model base classes and enum fields
- basic/JWT/SAML auth flows
- DRF base views, bulk actions, and import/export
- generating Django fixtures from Excel workbooks
