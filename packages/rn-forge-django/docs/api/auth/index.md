# Auth

`rn_forge.django.auth` is an installable Django app (label `rn_forge_django_auth`) providing
login flows and DRF authentication/authorization building blocks. See the
[Auth guide](../../guides/auth.md) for usage.
