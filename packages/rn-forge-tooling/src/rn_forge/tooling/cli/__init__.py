"""Tooling's own command surfaces.

The commands this package ships as executables — currently ``rn-forge-docs``
(:mod:`rn_forge.tooling.cli.docs`). The reusable command-line machinery they
are built from is not here: it lives in ``rn-forge-cli``, which this package
depends on.
"""
