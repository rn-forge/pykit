# API Reference

The API reference is generated from source docstrings with `mkdocstrings`.

The modules are listed in dependency order rather than alphabetically:
`context` and `exceptions` are what the rest build on, `problem` is what every
error response goes through, and `conformance` is the table both framework
packages are tested against.
