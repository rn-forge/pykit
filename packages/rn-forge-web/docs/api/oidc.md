# OIDC

Requires the `auth` extra:

```bash
uv add "rn-forge-web[auth]"
```

`rn_forge.web.oidc` is not re-exported from `rn_forge.web` — import it directly:

```python
from rn_forge.web.oidc import OidcAuthenticator
```

::: rn_forge.web.oidc
