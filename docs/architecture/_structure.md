# Architecture routing

Current relationships among packages and cross-package contracts belong here. Package internals belong in the owning package's docs.

Put architectural choices in `adr/`, planned work in `specs/`, and procedures in `guides/` or `runbooks/`. Use one subject per kebab-case page. The index links pages in reading order.
