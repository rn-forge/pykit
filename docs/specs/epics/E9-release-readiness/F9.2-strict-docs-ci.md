# F9.2 — Build documentation strictly in CI

This feature records the CI documentation-validation gap. It is for the maintainer who updates CI in a separate task.

**Status:** planned

**Readiness:** ready

**Readiness basis:** The strict-build change and observable acceptance are specified; execution still requires its listed dependency.

**Owner:** pykit.

**Depends on:** A clean strict build of the root and standalone sites.

**Source:** `docs/plans/reviews/docs-structure-review.md`, F4 and Open work register.

[Back to E9](index.md)

## Stories

### S9.2.1 — Make the docs job strict

**Status:** planned

**Acceptance:** CI runs a strict root MkDocs build after the documentation tree resolves its links.

## Acceptance

- The documentation CI job runs MkDocs with `--strict`.
- Root and seven standalone package sites resolve their intended links.

The current root CI command omits `--strict`. This documentation batch does not edit CI.
