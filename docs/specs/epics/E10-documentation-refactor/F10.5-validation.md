# F10.5 — Validate documentation

This feature records the final consistency and build checks. It is for the documentation maintainer handling Batch 5.

**Status:** done

**Owner:** pykit documentation refactor.

**Depends on:** [F10.4](F10.4-navigation-consumers.md).

**Source:** `docs/plans/reviews/docs-structure-review.md`, Batch 5.

[Back to E10](index.md)

## Stories

### S10.5.1 — Validate the complete docs tree

**Status:** done

**Acceptance:** Root and seven standalone sites build strictly and the final scope check passes.

## Acceptance

- Root and seven standalone MkDocs sites build strictly using existing dependencies.
- Links, status claims, source maps and documentation-only scope pass final checks.
- Any unavailable dependency or remaining build warning is reported with its cause.

## Validation record

On 2026-09-24, the root and all seven standalone package sites built with `mkdocs build --strict` using the existing `.venv/bin/mkdocs`. Each command exited 0. The builds emitted no MkDocs warnings or missing-anchor diagnostics. MkDocs Material printed its separate MkDocs 2.0 advisory.

The rendered root site has all seven package homes, guides and API references. Its 6,398 local fragment links resolve. Raw archives and routing files are absent from rendered output and search. The ledger has 309 rows; all 16 baseline sources match their recorded hashes, and all 12 moved plans are present in the archive. `git diff --check` passes for staged and unstaged changes. Q4, Q5, Q9 and Q10 remain open in their owning records.
