# Runbook routing

Repeated maintainer procedures with a decision or approval gate belong here. Each page lists ordered steps and the evidence needed at its gates.

Put routine development guidance in `guides/`, future work in `specs/`, and package release scope in `releases/`.
