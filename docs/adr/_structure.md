# Decision routing

Durable architectural choices belong in `ADR-<nnnn>.md`. Each record states the decision, consequences, influences, alternatives and source background. Pykit's sequence starts at 0001 and is independent of kiln's IDs.

Put task status in `specs/`, current behavior in `architecture/` or package docs, and historical evidence in `plans/context.md`. The index lists decisions by ID.
